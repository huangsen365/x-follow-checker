# X 互关检测 Chrome 插件开发指南

## 项目背景

在 X (原 Twitter) 平台上，用户之间经常发起"互关"活动，即互相关注以增加粉丝数量。然而存在一个普遍问题：部分用户在被关注后并不履行回关承诺，导致发起方白白付出关注却得不到回报。

目前没有便捷的工具帮助用户快速识别"谁没有回关我"，用户只能手动逐个检查，这在关注数量较多时极其低效。

## 核心诉求

开发一个 Chrome 浏览器插件，帮助 X 用户：

1. **一键获取自己关注的全部用户列表**
2. **识别哪些用户没有回关自己**
3. **提供清晰的可视化界面展示结果**

## 目标用户

- X 平台活跃用户
- 经常参与互关活动的用户
- 希望清理"单向关注"的用户
- 蓝 V 用户群体

## 功能需求

### 核心功能

1. **获取关注列表**
   - 获取当前登录用户关注的所有账号
   - 支持分页加载，处理大量关注数据
   - 显示加载进度

2. **互关状态检测**
   - 检测每个关注对象是否回关了自己
   - 区分显示：互相关注 / 单向关注（对方未回关）

3. **结果展示**
   - 列表形式展示所有关注用户
   - 支持按互关状态筛选
   - 显示用户基本信息（头像、用户名、简介等）

### 扩展功能（可选）

- 导出未回关用户列表（CSV/JSON）
- 批量取消关注未回关用户（需谨慎实现）
- 定期检测提醒
- 关注/取关历史记录

## 技术要点

### 插件架构

- **Manifest V3** 规范（Chrome 插件最新标准）
- Content Script：注入 X 页面获取认证状态
- Background Service Worker：处理 API 请求
- Popup 界面：用户交互入口

### 数据获取策略

- 利用用户已登录的会话状态
- 复用页面中的认证 Token
- 调用 X 平台的 API 接口获取数据
- 注意处理 API 请求频率限制

### 用户体验

- 插件图标显示在浏览器工具栏
- 点击图标弹出操作面板
- 操作简单，一键触发检测
- 加载过程有进度提示
- 结果清晰易读

## 约束与注意事项

### 合规性

- 仅获取用户自己的数据，不涉及他人隐私
- 不存储敏感信息到外部服务器
- 所有数据处理在本地完成
- 遵守 X 平台的使用条款

### 技术限制

- X 的 API 可能会变动，需要有容错机制
- 关注数量过多时需要分批请求
- 注意请求频率，避免触发限制
- 插件需要在 X 网站页面上才能工作

### 用户提示

- 首次使用需要在 X 网站登录状态下操作
- 关注数量很多时加载需要一定时间
- 建议适度使用，避免频繁请求

## 界面设计建议

### Popup 主界面

```
┌─────────────────────────────┐
│  X 互关检测助手              │
├─────────────────────────────┤
│                             │
│  [ 🔍 开始检测 ]             │
│                             │
│  关注总数: 520              │
│  互相关注: 380              │
│  未回关:   140              │
│                             │
├─────────────────────────────┤
│  [全部] [互关] [未回关]      │
├─────────────────────────────┤
│  👤 @user1 - 未回关         │
│  👤 @user2 - 互相关注       │
│  👤 @user3 - 未回关         │
│  ...                        │
└─────────────────────────────┘
```

### 状态指示

- 🟢 互相关注
- 🔴 对方未回关
- ⏳ 检测中
- ⚠️ 检测失败

## 开发里程碑

### Phase 1: 基础框架
- 搭建 Chrome 插件基础结构
- 实现 Popup 界面框架
- 验证能否获取 X 页面的认证信息

### Phase 2: 核心功能
- 实现获取关注列表功能
- 实现互关状态检测
- 完成结果列表展示

### Phase 3: 优化完善
- 添加加载进度显示
- 实现筛选和搜索功能
- 优化大数据量下的性能
- 错误处理和用户提示

### Phase 4: 扩展功能
- 数据导出功能
- 界面美化
- 其他增值功能

## 交付物

1. 完整的 Chrome 插件源代码
2. 插件安装使用说明
3. 打包好的 .crx 或可直接加载的插件文件夹

## 补充说明

本文档作为需求指南提供给 AI 辅助开发，具体技术实现细节（如 API 接口地址、请求参数格式等）需要在开发过程中通过调试和抓包确定，因为 X 平台的接口可能随时变化。

开发时建议：
1. 先在浏览器控制台验证 API 可行性
2. 采用渐进式开发，先实现最小可用版本
3. 保持代码结构清晰，便于后续维护更新
